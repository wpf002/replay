{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "${configDir}/dist", "rootDir": "${configDir}/src", "types": ["node"] },
  "include": ["${configDir}/src"],
  "exclude": ["${configDir}/src/**/*.test.ts", "${configDir}/src/test/**"]
}
